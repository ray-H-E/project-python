#include<iostream>
using namespace std;

struct Teacher
{
    /* data */
    //姓名
    string tName;

    //学生数组
    struct Student sArray[5];
};

struct Student
{
    /* data */
    //姓名
    string sName;

    //分数
    int score;
};

void allCreatSpace(struct Teacher tArray[], int len){

    string nameSpace = "ABCDEF"
    //赋值
    for (int i)
}

int main{
    //创建老师数组
    struct Teacher tArray[3];
    //赋值
    int len = sizeof(tArray) / 
    //打印


    return 0;
}
